# Commit Skill
1. Run the full test suite with `python -m pytest`
2. If all tests pass, `git add -A`, then ask user for commit message
3. Commit and push to current branch
4. If tests fail, report failures and do NOT commit
