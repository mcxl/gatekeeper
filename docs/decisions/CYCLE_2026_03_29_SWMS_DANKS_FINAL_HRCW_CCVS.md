# Refinement Decision Log
## Per-Cycle LBV Decision Record

---

### 1. Cycle Summary

- **Date:** 2026-03-29
- **Product mode:** SWMS
- **Benchmark stream:** 18 Danks Street Waterloo quote-to-SWMS benchmark
- **Benchmark case:** Robertson's Q50037-4
- **Cycle type:** Final HRCW / CCVS / framework-control refinement

### 2. Changes Applied

1. **QA monitoring pattern added:** New "qa" pattern in `_MONITORING_BY_HAZARD` — defect-list / specification / sign-off evidence. Added to pattern matcher so "check and fix defects" tasks get QA-appropriate monitoring instead of inheriting dust monitoring.

2. **QA evidence alignment:** Added `pattern == "qa"` check in monitoring evidence replacement logic.

### 3. Verification Results

| Check | Result |
|-------|--------|
| Tasks | 12/12 |
| CCVS distribution | SYS:2, WAH:4, SIL:3, CHM:3 — zero N/A |
| WAH percentage | 33% (scaffold erect/dismantle + green wall remove/reinstate) |
| CCVS-monitoring alignment | 0 mismatches |
| HRCW | `falling_2m` only — correct |
| Unsupported controls | CLEAN |
| Framework tasks | 1 standalone "Latent conditions check" — accepted (content is correct stop-work note; removing entire tasks has regression risk) |
| Footer | Filename, version, dates all populated from placeholders |
| Tests | 341 passing |

### 4. Remaining Agent-Level Items (not deterministically fixable)

- Standalone "Latent conditions check" task sometimes generated by decomposer — content is correct (stop-work note, not active survey) but would ideally be integrated as hold-point logic. Removing tasks mid-pipeline is too risky.
- Fibre cement sometimes lumped with masonry/render painting — agent variability.
- Task count varies 11-12 between runs.

### 5. End-of-Cycle Decision

- **Decision:** STRONG WORKING DRAFT — deterministic layer at practical limit
- **Why:** All material HRCW, CCVS, and control checks pass. The remaining issues (standalone latent-condition task, fibre cement lumping, task count variability) are inherent to LLM generation and cannot be resolved with more deterministic post-processing. The output is a strong working draft suitable for consultant review and site-specific completion. Further refinement cycles on the deterministic layer would produce diminishing returns.

### 6. One-Line Outcome

Final HRCW/CCVS refinement: QA monitoring pattern added, zero N/A, zero mismatches, 33% WAH. 341 tests. Decision: strong working draft, deterministic layer at practical limit.
