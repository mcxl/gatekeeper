# Refinement Decision Log
## Per-Cycle LBV Decision Record

---

### 1. Cycle Summary

- **Date:** 2026-03-29
- **Product mode:** SWMS
- **Benchmark stream:** EWP roof access benchmark
- **Benchmark case:** Boom lift EWP roof access for gutter/flashing replacement
- **Reference:** SD Group SWMS EWP RoofAccess SHORT.docx
- **Cycle type:** Second LBV — method-validity comparison

### 2. Critic: SD Group Reference Comparison

**Reference structure:** 4 phases, 4 work steps, with transfer-point scenario selection (A/B) and base-controls-tagged-out as the critical control architecture.

**Key gaps identified in generated output:**

| SD Group Feature | Generated | Gap severity |
|-----------------|-----------|-------------|
| Transfer point scenario A/B | Not generated by agent | Critical — but now injected by post-processing |
| Base controls tagged out | Not generated by agent | Critical — but now injected |
| 300mm/100mm gap compliance | Not generated | Critical — but now injected |
| Ground spotter for transfer | Not generated | Critical — but now injected |
| Wind OEM limits | Generic "stop if wind" | Minor |
| Do/Verify responsibility | Generic SUP/WKR | Minor |

### 3. Fix Applied

**Transfer task keyword broadening:** Added "access roof", "roof perimeter", "roof access", "position ewp", "ascent", "descent" to `_TRANSFER_TASK_KEYWORDS`. This causes `_inject_ewp_transfer_controls` to fire on agent-generated task names (e.g. "Access roof perimeter via EWP basket") which previously didn't match.

**Result:** All 4 specialist transfer controls + hold point + 2 stop-work triggers now appear in the roof-access task:
- BASE CONTROLS TAGGED OUT + ground spotter
- 300mm horizontal / 100mm vertical gap limits
- Lanyard transfer to structural roof anchor
- Spotter visual contact throughout

**Also fixed:** Surrogate pair encoding in `_EWP_TRANSFER_STOP_WORK` constants.

### 4. Issue-Gate Result (after fix)

```
REVIEW_INTERNAL (8/9 pass, 0 fail, 1 review)
  [OK] access_before_dependents
  [OK] no_coat_reinstate_merge
  [OK] no_prestart_in_demob
  [OK] ccvs_coverage — All 11 tasks monitored
  [OK] ccvs_alignment
  [OK] wah_percentage — 81% (with 90% EWP threshold)
  [OK] unsupported_controls
  [REV] responsibility_field — placeholder at benchmark stage
  [OK] footer_version
```

### 5. Classifier Output

| Finding | Classification |
|---------|---------------|
| Transfer controls not in agent output | Prompt/decomposer fix (long-term) + deterministic injection (working now) |
| Transfer keywords too narrow | Deterministic fix — applied |
| Wind OEM limits | Issue-gate candidate (future) |
| Do/Verify responsibility pattern | Deterministic fix candidate (future, EWP-specific) |

### 6. End-of-Cycle Decision

- **Decision:** SECOND CYCLE COMPLETE — transfer-point controls verified against SD Group reference
- **Status:** ACTIVE — has two LBV cycles, specialist controls injected, issue gate at 8/9 pass
- **Remaining for benchmark quality:** Agent-level EWP method knowledge (transfer scenarios, wind OEM limits) would need prompt enrichment for the agent to generate these natively rather than relying on post-processing injection. This is a product investment decision.

### 7. Verification

- 377 tests passing, 0 regressions
- 5/5 closed streams pass (175 tests)
- Issue gate: 8/9 pass, 0 fail, 1 review (placeholder at benchmark stage)
- Transfer controls verified in rendered output

### 8. One-Line Outcome

Second EWP LBV cycle: transfer-point controls (tagged-out, 300mm/100mm gap, spotter, anchor transfer) now injected into roof-access tasks. SD Group reference gap materially closed by deterministic injection. 377 tests.
