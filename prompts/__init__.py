# prompts/ — Safe Method prompt behaviour layers
#
# System-wide behaviour + mode-specific behaviour, composed into
# each agent's system prompt. Agents import from here rather than
# duplicating behavioural rules.
